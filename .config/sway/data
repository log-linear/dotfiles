# Daemonize foot
exec foot --server --font="JetBrainsMono Nerd Font:size=9"

bindsym $mod+shift+return exec notify-send "foot server launched" && foot --server --font="JetBrainsMono Nerd Font:size=9"

workspace 1
exec $term
exec $term

include ~/.config/sway/outputs
